<%= form_for(assignment) do |f| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :assignment_source %>
    <%= f.text_field :assignment_source %>
  </div>

  <div class="field">
    <%= f.label :assignment_file %>
    <%= f.text_field :assignment_file %>
  </div>

  <div class="field">
    <%= f.label :course_module_id %>
    <%= f.collection_select :course_module_id, CourseModule.all, :id, :module_num %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
