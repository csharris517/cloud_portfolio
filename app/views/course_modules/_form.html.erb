<%= form_for(course_module) do |f| %>
  <% if course_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_module.errors.count, "error") %> prohibited this course_module from being saved:</h2>

      <ul>
      <% course_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :module_num %>
    <%= f.text_field :module_num %>
  </div>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.collection_select :course_id, Course.all, :id, :course_num %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
